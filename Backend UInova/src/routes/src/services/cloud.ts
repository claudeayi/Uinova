// Placeholder pour gestion cloud/stockage
export async function uploadToCloud(filePath: string) {
  // À implémenter selon le service cloud choisi (S3, Cloudinary, etc.)
  // Ex: return await cloudinary.uploader.upload(filePath);
  return { url: filePath };
}
