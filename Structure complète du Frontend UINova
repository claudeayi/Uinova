src/
├─ App.tsx                   # Routing global V3 (toutes pages, lazy loading, layout)
├─ main.tsx                  # Entrée React
├─ index.css                 # Styles globaux (Tailwind, dark mode)
│
├─ layouts/
│  ├─ Navbar.tsx             # Navigation (desktop + mobile + dark mode)
│  └─ DashboardLayout.tsx    # Layout cockpit (navbar + sidebar + contenu)
│
├─ context/
│  └─ ProjectContext.tsx     # Contexte projet actif (id, page courante, etc.)
│
├─ hooks/
│  ├─ useAuth.ts             # Auth utilisateur (me, login, logout, role)
│  ├─ useToast.ts            # Notifications centralisées (react-hot-toast wrapper)
│  └─ useTheme.ts            # Dark/Light mode switch
│
├─ services/
│  ├─ http.ts                # Axios configuré (baseURL + interceptor JWT)
│  ├─ auth.ts                # Login / Register / Me
│  ├─ projects.ts            # CRUD projets + sauvegarde
│  ├─ marketplace.ts         # Marketplace (templates, publish, purchase)
│  ├─ payments.ts            # Paiements (Stripe, PayPal, Mobile Money, CinetPay)
│  ├─ badges.ts              # Badges utilisateurs (progression, succès)
│  ├─ notifications.ts       # Notifications (in-app + push)
│  ├─ upload.ts              # Uploads fichiers (images, docs)
│  ├─ ai.ts                  # ⚡ IA générative (chat, UI suggest, auto-code)
│  └─ admin.ts               # ⚡ Admin services (users, projects, logs, replays)
│
├─ components/
│  ├─ base/
│  │   └─ Button.tsx         # Bouton générique (primary, secondary, danger)
│  │
│  ├─ advanced/
│  │   ├─ Card.tsx           # Carte réutilisable
│  │   ├─ Carousel.tsx       # Carousel interactif
│  │   └─ Modal.tsx          # Modal générique
│  │
│  ├─ onboarding/
│  │   └─ OnboardingCoach.tsx # Assistant onboarding utilisateurs
│  │
│  ├─ editor/
│  │   ├─ EditorWrapper.tsx   # Encapsule EditorPage
│  │   ├─ LivePreview.tsx     # Preview readonly (utilisé par PreviewPage)
│  │   ├─ ComponentPalette.tsx # Palette de composants drag&drop
│  │   ├─ LiveEditor.tsx      # Canvas principal (gestion des composants)
│  │   ├─ AssetLibrary.tsx    # 📚 Bibliothèque d’assets (images + preview hover)
│  │   └─ ShareModal.tsx      # Modal de partage
│  │
│  └─ projects/
│      └─ ProjectSelector.tsx # Sélecteur projets multi-pages
│
├─ routes/
│  └─ ProtectedRoute.tsx      # Route protégée par rôle (admin/user/premium)
│
├─ pages/
│  ├─ Dashboard.tsx           # Cockpit principal utilisateur
│  ├─ ProjectsPage.tsx        # Liste des projets
│  ├─ ProjectDetailPage.tsx   # Détails projet (stats, badges, progression)
│  ├─ EditorPage.tsx          # ⚡ Editeur complet (v3.8, palette, assets, IA)
│  ├─ PreviewPage.tsx         # Preview live readonly (iframe + rendu JSON)
│  ├─ MarketplacePage.tsx     # Marketplace publique
│  ├─ TemplatePage.tsx        # Détail template marketplace
│  ├─ MarketplaceManager.tsx  # Admin gestion marketplace
│  ├─ PricingPage.tsx         # Plans Freemium / Premium
│  ├─ PaymentPage.tsx         # Page paiement (Stripe, PayPal, Mobile Money)
│  ├─ DeployPage.tsx          # Déploiement projet (hosting + logs)
│  ├─ ReplayPage.tsx          # Replays collaboratifs
│  ├─ ARPreviewPage.tsx       # Preview en réalité augmentée (mock AR viewer)
│  ├─ MonitoringPage.tsx      # Monitoring système (CPU, mémoire, uptime)
│  ├─ AIAssistantPage.tsx     # ⚡ Assistant IA conversationnel (Deepseek natif)
│  │
│  ├─ LoginPage.tsx           # Connexion
│  ├─ RegisterPage.tsx        # Inscription
│  ├─ NotFound.tsx            # 404
│  │
│  └─ admin/                  
│       ├─ UsersAdmin.tsx     # Gestion utilisateurs (roles, plans)
│       ├─ ProjectsAdmin.tsx  # Gestion projets admin
│       ├─ LogsAdmin.tsx      # Audit logs
│       └─ ReplaysAdmin.tsx   # Gestion replays collaboratifs
│
└─ pages/AdminPanel.tsx       # ⚡ Hub principal Admin (liens vers toutes sections)
